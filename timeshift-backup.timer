[Unit]
Description=Timeshift snapshot (non-btrfs boxes)

[Timer]
# Before the daily borg/BIT and the restore-readiness check.
OnCalendar=*-*-* 02:30:00
RandomizedDelaySec=15m
Persistent=true

[Install]
WantedBy=timers.target
